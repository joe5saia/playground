Each project has it's own directory. This repo as a whole is a mono repo. Each directory will have it's own Readme and AGENTS.md file. Each CLAUDE.md file will refrence the AGENTS.md file. Each directory will have its own project management. This repo uses uv for python dependency management and targets python 3.14. For javeascript it always uses the latest version of typescript and node.
